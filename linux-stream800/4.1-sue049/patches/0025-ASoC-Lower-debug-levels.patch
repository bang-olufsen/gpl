From 22ed53d03f3129d0e598b9b744dfcbe0bb064e1d Mon Sep 17 00:00:00 2001
From: Martin Elias <martin.elias@streamunlimited.com>
Date: Wed, 17 Jan 2018 10:35:02 +0100
Subject: [PATCH] ASoC: Change log level to debug

Signed-off-by: Martin Elias <martin.elias@streamunlimited.com>
---
 sound/soc/davinci/am33xx-s800.c | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/sound/soc/davinci/am33xx-s800.c b/sound/soc/davinci/am33xx-s800.c
index ed29328..2df8d47 100644
--- a/sound/soc/davinci/am33xx-s800.c
+++ b/sound/soc/davinci/am33xx-s800.c
@@ -79,7 +79,7 @@ static int am33xx_s800_setup_mcasp(struct snd_pcm_substream *substream, snd_pcm_
 	bool is_dsd = (format == SNDRV_PCM_FORMAT_DSD_U8);
 
 	if (!serial_config) {
-		dev_info(card->dev, "Serial configuration is empty, skipping reconfiguration\n");
+		dev_dbg(card->dev, "Serial configuration is empty, skipping reconfiguration\n");
 		return 0;
 	}
 
@@ -308,7 +308,7 @@ static int am33xx_s800_common_hw_params(struct snd_pcm_substream *substream,
 	/* Codec MCLK */
 	ret = snd_soc_dai_set_sysclk(codec_dai, 0, mclk, SND_SOC_CLOCK_IN);
 	if (ret < 0) {
-		dev_info(card->dev, "Unsupported codec dai MLCK : %d\n", ret);
+		dev_dbg(card->dev, "Unsupported codec dai MLCK : %d\n", ret);
 		/* intentionally ignore errors - the codec driver may not care, at least give a warning */
 	}
 
@@ -344,7 +344,7 @@ static int am33xx_s800_common_hw_params(struct snd_pcm_substream *substream,
 		return ret;
 	}
 
-	dev_info(card->dev, "Configured common HW params, RATE %d, MCLK %d, BCLK %d", rate, mclk, bclk);
+	dev_dbg(card->dev, "Configured common HW params, RATE %d, MCLK %d, BCLK %d", rate, mclk, bclk);
 
 	return 0;
 }
-- 
1.9.1

